---
import BaseLayout from '../layouts/BaseLayout.astro';
import SiteHeader from '../components/SiteHeader.astro';
import { href } from '../lib/href';
---

<BaseLayout title="페이지를 찾을 수 없어요 | 복지정보.com" description="요청하신 페이지를 찾을 수 없습니다." canonicalPath="/" noindex>
  <SiteHeader />
  <section class="mx-auto mt-10 max-w-2xl rounded-2xl border border-slate-200 bg-white p-8 text-center shadow-sm">
    <h1 class="text-2xl font-bold text-slate-900 sm:text-3xl">페이지를 찾을 수 없어요</h1>
    <p class="mt-3 text-sm text-slate-600">링크가 바뀌었거나 삭제됐을 수 있어요. 아래 경로에서 다시 찾아보세요.</p>
    <div class="mt-6 flex flex-wrap items-center justify-center gap-2">
      <a href={href('/')} class="rounded-lg bg-slate-900 px-4 py-2 text-sm font-semibold text-white hover:bg-slate-700">홈으로</a>
      <a href={href('/search/')} class="rounded-lg bg-slate-100 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-200">검색으로</a>
      <a href={href('/updates/')} class="rounded-lg bg-slate-100 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-200">업데이트</a>
    </div>
  </section>
</BaseLayout>
